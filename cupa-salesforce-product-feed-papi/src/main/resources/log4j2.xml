<?xml version="1.0" encoding="utf-8"?>
<Configuration>

	<!--These are some of the loggers you can enable. There are several more 
		you can find in the documentation. Besides this log4j configuration, you 
		can also use Java VM environment variables to enable other logs like network 
		(-Djavax.net.debug=ssl or all) and Garbage Collector (-XX:+PrintGC). These 
		will be append to the console, so you will see them in the mule_ee.log file. -->

	<Appenders>
		<!-- ROLLING FILE APPENDER -->
		<RollingFile
			name="file"
			fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}cupa-salesforce-product-feed-papi.log"
			filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}cupa-salesforce-product-feed-papi-%i.log">
			<PatternLayout
				pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n" />
			<SizeBasedTriggeringPolicy size="10 MB" />
			<DefaultRolloverStrategy max="10" />
		</RollingFile>

		<!-- EXABEAM APPENDER -->
		<Syslog
			name="exabeamAppender"
			format="RFC5424"
			host="${sys:exabeam.host}"
			port="${sys:exabeam.port}"
			protocol="TCP"
			appName="cupa-salesforce-product-feed-papi-${sys:mule.env}"
			includeMDC="true"
			newLine="true"
			id="App">
			
			<JSONLayout 
				complete="true"
				compact="false"
				eventEol="true"
				objectMessageAsJsonObject="true" />
		</Syslog>

		<!-- CLOUDHUB APPENDER -->
		<Log4J2CloudhubLogAppender
			name="cloudhub"
			addressProvider="com.mulesoft.ch.logging.DefaultAggregatorAddressProvider"
			applicationContext="com.mulesoft.ch.logging.DefaultApplicationContext"
			appendRetryIntervalMs="${sys:logging.appendRetryInterval}"
			appendMaxAttempts="${sys:logging.appendMaxAttempts}"
			batchSendIntervalMs="${sys:logging.batchSendInterval}"
			batchMaxRecords="${sys:logging.batchMaxRecords}"
			memBufferMaxSize="${sys:logging.memBufferMaxSize}"
			journalMaxWriteBatchSize="${sys:logging.journalMaxBatchSize}"
			journalMaxFileSize="${sys:logging.journalMaxFileSize}"
			clientMaxPacketSize="${sys:logging.clientMaxPacketSize}"
			clientConnectTimeoutMs="${sys:logging.clientConnectTimeout}"
			clientSocketTimeoutMs="${sys:logging.clientSocketTimeout}"
			serverAddressPollIntervalMs="${sys:logging.serverAddressPollInterval}"
			serverHeartbeatSendIntervalMs="${sys:logging.serverHeartbeatSendIntervalMs}"
			statisticsPrintIntervalMs="${sys:logging.statisticsPrintIntervalMs}">

			<PatternLayout
				pattern="%c{1} %-5p %d [%t] [event: %X{correlationId,api.name}] %c: %m%n" />
		</Log4J2CloudhubLogAppender>

	</Appenders>

	<Loggers>
		<!-- Http Logger shows wire traffic on DEBUG -->
		<!--AsyncLogger name="org.mule.service.http.impl.service.HttpMessageLogger" 
			level="DEBUG"/ -->
		<AsyncLogger
			name="org.mule.service.http"
			level="WARN" />
		<AsyncLogger
			name="org.mule.extension.http"
			level="WARN" />

		<!-- Mule logger -->
		<AsyncLogger
			name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor"
			level="INFO" />

		<!-- Mule classes -->
		<AsyncLogger
			name="org.mule"
			level="INFO" />
		<AsyncLogger
			name="com.mulesoft"
			level="INFO" />

		<!-- ENABLE ROLLING FILE APPENDER AND DISABLE EXABEAM AND CLOUDHUB APPENDER 
			WHEN TESTING IN LOCAL -->
		<AsyncRoot level="INFO">
			<!-- <AppenderRef ref="file" /> -->
			<AppenderRef ref="exabeamAppender" />
			<AppenderRef ref="cloudhub" />
		</AsyncRoot>
	</Loggers>

</Configuration>
